"""Droideka GPU-server utilities (Pure Pursuit, path export, live SLAM wiring).

DROID-SLAM itself is expected to be installed separately; set DROID_SLAM_ROOT
to your DROID-SLAM checkout when running scripts that import ``droid`` or
``droid_backends`` (see live_slam.py, export_ply.py).
"""
